# Variáveis do Smart Responder (Supabase Functions)
# Copie para .env e preencha com seus valores
SMART_RESPONDER_URL=https://<PROJECT-REF>.supabase.co/functions/v1/smart-responder

# Preferido (alinhado ao n8n):
# - Authorization usa um JWT válido (USER ACCESS TOKEN ou service_role)
# - apikey usa a chave pública (anon key)
SMART_RESPONDER_AUTH=Bearer <USER_ACCESS_TOKEN_ou_service_role>
SMART_RESPONDER_APIKEY=<ANON_PUBLIC_KEY>

# Compatibilidade antiga (se preferir manter apenas um campo):
# - Se definir somente SMART_RESPONDER_TOKEN, ele será usado como Authorization
# - Dica: se for um JWT sem "Bearer ", o código adiciona automaticamente
SMART_RESPONDER_TOKEN=

# Caminho do arquivo de prompt do agente (opcional)
AGENT_PROMPT_PATH=prompts/agent_system.md

# Pré-resolvedor do agente (proativo EAN→preço/estoque via Supabase)
# true: usa o smart-responder para sugerir opções e buscar preço por EAN
# false: pula direto para o agente LLM (sem resolver EAN automaticamente)
PRE_RESOLVER_ENABLED=true